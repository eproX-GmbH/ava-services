import { SettingsSkills } from "../SettingsSkills";

// Skills-Tab — Liste, Editor, Trust-Dialog, Import/Export.
// Delegiert komplett an die bestehende SettingsSkills-Komponente.
export function SkillsTab() {
  return <SettingsSkills />;
}
