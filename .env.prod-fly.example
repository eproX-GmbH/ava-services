# Production on fly.io — cloud-parity stack (Step 4 keeps this path working
# alongside the Electron bundle per D2/D7). Secrets managed via fly secrets.

DATABASE_URL=
AMQP_URL=
JWKS_URI=
JWKS_KID=

QUEUE_DRIVER=rabbitmq
DB_CLIENT=direct

LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
OPENAI_API_KEY=

ELASTIC_URL=
ELASTIC_API_KEY=

LOGLEVEL=info
NODE_ENV=production
